{
  "name": "virtual-meetup-cross-browser",
  "version": "1.0.0",
  "private": true,
  "description": "Layer 4 UI tests — Selenium WebDriver cross-browser smoke against a deployed/staging stack via Selenium Grid or a cloud grid (BrowserStack/Sauce). Skips cleanly when TARGET_URL/grid creds are unset. Kept OUTSIDE frontend/.",
  "scripts": {
    "test": "node run.js",
    "test:local-grid": "cross-env SELENIUM_REMOTE_URL=http://localhost:4444/wd/hub node run.js"
  },
  "dependencies": {
    "selenium-webdriver": "^4.27.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
