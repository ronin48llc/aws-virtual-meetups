'use strict';

// Keep this list short and high-value — Layer 4 is breadth (engines), not depth.
module.exports = {
  'home renders hero': require('./home.flow'),
  'event list section mounts': require('./event-list.flow'),
};
